static void Main(string[] args)
    {
        for(int row = 0; row <8; row++)
        {
            for(int col =0; col<8; col++)
            {
                Console.Write((col + row) % 2 == 0 ? 'X' : 'O');
            }
            Console.WriteLine();
        }
    }
